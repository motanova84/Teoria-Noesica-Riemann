name: 🌊 QCAL Resonance Check

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  test-resonance:
    name: 🧪 Verificar Frecuencia y Gráfico QCAL
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repositorio
        uses: actions/checkout@v4

      - name: 📦 Instalar dependencias
        run: |
          python3 -m pip install --upgrade pip
          pip install matplotlib numpy

      - name: ✅ Ejecutar verificación de frecuencia
        run: python3 scripts/verifica_frecuencia.py

      - name: 📈 Generar gráfico de resonancia
        run: python3 scripts/genera_grafico.py

      - name: 📤 Subir gráfico como artefacto
        uses: actions/upload-artifact@v4
        with:
          name: grafico-resonancia-qcal
          path: resultados_qcal/grafico_resonancia.png


